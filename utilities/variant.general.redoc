[section#variant.general
    [:en] In general
    [:zh_CN] 概述
]

[para]
[:en] A variant object holds and manages the lifetime of a value.
[:zh_CN] 变体对象持有并管理一个值的生存期

[:en] If the [`variant] holds a value, that value[=']s type has to be one of the
template argument types given to [`variant].
[:zh_CN] 如果 [`variant] 持有一个值，则这个值的类型必须是为这个 [`variant] 给定
的模板实参之一。

[:en] These template arguments are called alternatives.
[:zh_CN] 这些模板实参被称为其候选项。
