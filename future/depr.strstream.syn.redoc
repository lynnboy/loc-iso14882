[section#depr.strstream.syn
    [:en] Header [`strstream] synopsis
    [:zh_CN] 头文件 [`strstream] 纲要
]

[para]
[%@header strstream]
[:en] The header [`<strstream>] defines types that associate stream buffers with
character array objects and assist reading and writing such objects.
[:zh_CN] 头文件 [`<strstream>] 定义了一些类型，将流缓冲区和字符数组对象关联起来，
并辅助读取和写入这些对象。

[codeblock]
namespace std {
  class strstreambuf;
  class istrstream;
  class ostrstream;
  class strstream;
}
[codeblock:end]
