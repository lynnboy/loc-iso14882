[section#concepts.same
    [:en] Concept [`:c same_as]
    [:zh_CN] 概念 [`:c same_as]
]

[codeblock:declaration]
template<class T, class U>
  concept [[redoc("*:c@def", "n>")]]same-as-impl = is_same_v<T, U>;    // [=expos]

template<class T, class U>
  concept [[redoc("`:c@def", "i>")]]same_as = [[redoc("*:c", "n>")]]same-as-impl<T, U> && [[redoc("*:c", "n>")]]same-as-impl<U, T>;
[codeblock:end]

[div:description]
[para]
[begin:note]
[:en] [`[`:c same_as]<T, U>] subsumes [`[`:c same_as]<U, T>] and vice versa.
[:zh_CN] [`[`:c same_as]<T, U>] 归入 [`[`:c same_as]<U, T>]，反之亦然。
[end:note]
[div:end]
