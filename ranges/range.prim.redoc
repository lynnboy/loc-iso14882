[section#range.prim
    [:en] Range primitives
    [:zh_CN] 范围原语
]

[para]
[:en] In addition to being available via inclusion of the [`<ranges>] header,
the customization point objects in [#range.prim] are available when
[`<iterator>] is included.
[:zh_CN] 除了通过包含 [`<ranges>] 头文件以外，包含 [`<iterator>] 也可以使得
[#range.prim] 中的各个定制化点可以使用。

[include range.prim.size]

[include range.prim.empty]

[include range.prim.data]

[include range.prim.cdata]
