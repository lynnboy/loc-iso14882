[section#range.empty.overview
    [:en] Overview
    [:zh_CN] 概述
]

[para]
[:en] [`empty_view] produces a view of no elements of a particular type.
[:zh_CN] [`empty_view] 产生某个特定类型的视图，它没有任何元素。

[para]
[begin:example]
[codeblock]
auto e = views::empty<int>;
static_assert(ranges::empty(e));
static_assert(0 == e.size());
[codeblock:end]
[end:example]
