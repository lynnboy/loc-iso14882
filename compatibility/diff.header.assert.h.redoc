[section#diff.header.assert.h
    [:en] Header [`<assert.h>]
    [:zh_CN] 头文件 [`<assert.h>]
]

[%@header assert.h]

[para]
[:en] The token [`static_assert] is a keyword in this International Standard
([#lex.key]).
[:zh_CN] 记号 [`static_assert] 在本国际标准中是一个关键字（[#lex.key]）。

[:] [%@header cassert]
[:en] It does not appear as a macro name defined in [`<cassert>]
([#cassert.syn]).
[:zh_CN] 它并不作为 [`<cassert>]（[#cassert.syn]）中所定义的宏名。
