[section#diff.header.assert.h
    [:en] Header [`<assert.h>]
    [:zh_CN] 头文件 [`<assert.h>]
]

[%@hdr assert.h]

[para]
[:en] The token [`:key static_assert] is a keyword in [=Cpp] ([#lex.key]).
[:zh_CN] 记号 [`:key static_assert] 在 [=Cpp] 中是一个关键字（[#lex.key]）。

[:en] It does not appear as a macro name defined in [?libheader@ref cassert].
[:zh_CN] 它并不作为 [?libheader@ref cassert] 中所定义的宏名。
