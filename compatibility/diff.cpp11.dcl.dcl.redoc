[section#diff.cpp11.dcl.dcl
    [:en] [#dcl.dcl]: declarations
    [:zh_CN] [#dcl.dcl]：声明式
]

[para:~] [#dcl.constexpr]
[=Change]
[:en] [`constexpr] non-static member functions are not implicitly [`const]
member functions.
[:zh_CN] [`constexpr] 的非静态成员函数并不隐含为 [`const] 成员函数。

[=Rationale]
[:en] Necessary to allow [`constexpr] member functions to mutate the object.
[:zh_CN] 要允许 [`constexpr] 成员函数改动对象要求如此。

[=Effect]
[:en] Valid [=CppXI] code may fail to compile in this International Standard.
[:zh_CN] 有效的 [=CppXI] 代码在本国际标准种可能无法编译。

[:en] For example, the following code is valid in [=CppXI] but invalid in this
International Standard because it declares the same member function twice with
different return types:
[:zh_CN] 例如，以下代码在 [=CppXI] 中有效但在本国际标准中无效，因为它两次声明了
相同的成员函数并带有不同的返回类型：

[codeblock]
struct S {
  constexpr const int &f();
  int &f();
};
[codeblock:end]
