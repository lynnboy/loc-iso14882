[section#deque.capacity
    [:en] Capacity
    [:zh_CN] 容量
]

[%@library resize[!deque]]
[codeblock:declaration]
void resize(size_type sz);
[codeblock:end]

[enter:description]
[para:effects]
[:en] If [`sz < size()], erases the last [`size() - sz] elements from the
sequence.
[:zh_CN] 如果 [`sz < size()]，则从序列中清除最后 [`size() - sz] 个元素。

[:en] Otherwise, appends [`sz - size()] default-inserted elements to the
sequence.
[:zh_CN] 否则，向序列中追加 [`sz - size()] 个默认插入的元素。

[para:requires]
[:en] [`T] shall be [`MoveInsertable] and [`DefaultInsertable] into [`*this].
[:zh_CN] [`T] 应当为向 [`*this] 中 [`MoveInsertable] 且 [`DefaultInsertable]。
[exit:description]

[%@library resize[!deque]]
[codeblock:declaration]
void resize(size_type sz, const T& c);
[codeblock:end]

[enter:description]
[para:effects]
[:en] If [`sz < size()], erases the last [`size() - sz] elements from the
sequence.
[:zh_CN] 如果 [`sz < size()]，则从序列中清除最后 [`size() - sz] 个元素。

[:en] Otherwise, appends [`sz - size()] copies of [`c] to the sequence.
[:zh_CN] 否则，向序列中追加 [`sz - size()] 个 [`c] 的副本。

[para:requires]
[:en] [`T] shall be [`CopyInsertable] into [`*this].
[:zh_CN] [`T] 应当为向 [`*this] 中 [`CopyInsertable]。
[exit:description]

[%@library shrink_to_fit[!deque]]
[%@library deque[!shrink_to_fit]]
[codeblock:declaration]
void shrink_to_fit();
[codeblock:end]

[enter:description]
[para:requires]
[:en] [`T] shall be [`MoveInsertable] into [`*this].
[:zh_CN] [`T] 应当为向 [`*this] 中 [`MoveInsertable]。

[para:effects]
[:en] [`shrink_to_fit] is a non-binding request to reduce memory use but does
not change the size of the sequence.
[:zh_CN] [`shrink_to_fit] 是非强制性的请求，以缩减内存用量但不改变序列的大小。

[enter:note]
[:en] The request is non-binding to allow latitude for implementation-specific
optimizations.
[:zh_CN] 该请求不具约束力，以允许进行特定于实现的优化。
[exit:note]

[:en] If an exception is thrown other than by the move constructor of a
non-[`CopyInsertable] [`T] there are no effects.
[:zh_CN] 如果抛出了异常但并非由某个非 [`CopyInsertable] 的 [`T] 的移动构造函数所
抛出，则没有效果。

[para:complexity]
[:en] Linear in the size of the sequence.
[:zh_CN] 对于序列的大小为线性。

[para:remarks]
[:en] [`shrink_to_fit] invalidates all the references, pointers, and iterators
referring to the elements in the sequence as well as the past-the-end iterator.
[:zh_CN] [`shrink_to_fit] 将无效化指代这个序列中的元素的所有引用、指针和迭代器，
以及‘越过末尾’迭代器。
[exit:description]
