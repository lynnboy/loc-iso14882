[section#associative.general
    [:en] In general
    [:zh_CN] 综述
]

[para]
[:en] The header [?libheader map] defines the class templates [`map] and
[`multimap]; the header [?libheader set] defines the class templates [`set] and
[`multiset].
[:zh_CN] 头文件 [?libheader map] 定义了类模板 [`map] 和 [`multimap]；头文件
[?libheader set] 定义了类模板 [`set] 和 [`multiset]。

[para]
[:en] The following exposition-only alias templates may appear in deduction
guides for associative containers:
[:zh_CN] 以下各仅用于阐释的别名模板可能用于关联容器的推断导引之中：

[codeblock:synopsis]
template<class InputIterator>
  using [[redoc("^>")]]iter-value-type =
    typename iterator_traits<InputIterator>::value_type;                // [=expos]
template<class InputIterator>
  using [[redoc("^>")]]iter-key-type = remove_const_t<
    typename iterator_traits<InputIterator>::value_type::first_type>;   // [=expos]
template<class InputIterator>
  using [[redoc("^>")]]iter-mapped-type =
    typename iterator_traits<InputIterator>::value_type::second_type;   // [=expos]
template<class InputIterator>
  using [[redoc("^>")]]iter-to-alloc-type = pair<
    add_const_t<typename iterator_traits<InputIterator>::value_type::first_type>,
    typename iterator_traits<InputIterator>::value_type::second_type>;  // [=expos]
[codeblock:end]
