[section#c.math.fpclass
    [:en] Classification / comparison functions
    [:zh_CN] 分类 / 比较函数
]

[para]
[:en] The classification / comparison functions behave the same as the C macros
with the corresponding names defined in the C standard library.
[:zh_CN] 分类 / 比较函数与 C 标准程序库中所定义的具有对应名字的 C 宏的行为相同。

[:en] Each function is overloaded for the three floating-point types.
[:zh_CN] 每个函数都为三种浮点类型提供了重载。

[para:xref@c] 7.12.3, 7.12.4
