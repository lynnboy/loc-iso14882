[section#cmath.syn
    [:en] Header [`<cmath>] synopsis
    [:zh_CN] 头文件 [`<cmath>] 纲要
]

[%@hdr@def cmath]
[codeblock:synopsis]
#define HUGE_VAL[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define HUGE_VALF[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define HUGE_VALL[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define INFINITY[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define NAN[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define FP_INFINITE[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define FP_NAN[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define FP_NORMAL[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define FP_SUBNORMAL[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define FP_ZERO[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define FP_FAST_FMA[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define FP_FAST_FMAF[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define FP_FAST_FMAL[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define FP_ILOGB0[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define FP_ILOGBNAN[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define MATH_ERRNO[[redoc("`:m<")]] [[redoc("[=seebelow]")]]
#define MATH_ERREXCEPT[[redoc("`:m<")]] [[redoc("[=seebelow]")]]

#define math_errhandling[[redoc("`:m<")]] [[redoc("[=seebelow]")]]

namespace std {
  using [[redoc("`:lib>")]]float_t = [[redoc("[=seebelow]")]];
  using [[redoc("`:lib>")]]double_t = [[redoc("[=seebelow]")]];

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]acos([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]acosf(float x);
  constexpr long double         [[redoc("`:lib>")]]acosl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]asin([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]asinf(float x);
  constexpr long double         [[redoc("`:lib>")]]asinl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]atan([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]atanf(float x);
  constexpr long double         [[redoc("`:lib>")]]atanl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]atan2([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]atan2f(float y, float x);
  constexpr long double         [[redoc("`:lib>")]]atan2l(long double y, long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]cos([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]cosf(float x);
  constexpr long double         [[redoc("`:lib>")]]cosl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]sin([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]sinf(float x);
  constexpr long double         [[redoc("`:lib>")]]sinl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]tan([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]tanf(float x);
  constexpr long double         [[redoc("`:lib>")]]tanl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]acosh([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]acoshf(float x);
  constexpr long double         [[redoc("`:lib>")]]acoshl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]asinh([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]asinhf(float x);
  constexpr long double         [[redoc("`:lib>")]]asinhl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]atanh([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]atanhf(float x);
  constexpr long double         [[redoc("`:lib>")]]atanhl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]cosh([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]coshf(float x);
  constexpr long double         [[redoc("`:lib>")]]coshl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]sinh([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]sinhf(float x);
  constexpr long double         [[redoc("`:lib>")]]sinhl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]tanh([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]tanhf(float x);
  constexpr long double         [[redoc("`:lib>")]]tanhl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]exp([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]expf(float x);
  constexpr long double         [[redoc("`:lib>")]]expl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]exp2([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]exp2f(float x);
  constexpr long double         [[redoc("`:lib>")]]exp2l(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]expm1([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]expm1f(float x);
  constexpr long double         [[redoc("`:lib>")]]expm1l(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]frexp([[redoc("^>")]]floating-point-type value, int* exp);
  constexpr float               [[redoc("`:lib>")]]frexpf(float value, int* exp);
  constexpr long double         [[redoc("`:lib>")]]frexpl(long double value, int* exp);

  constexpr int [[redoc("`:lib>")]]ilogb([[redoc("^>")]]floating-point-type x);
  constexpr int [[redoc("`:lib>")]]ilogbf(float x);
  constexpr int [[redoc("`:lib>")]]ilogbl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]ldexp([[redoc("^>")]]floating-point-type x, int exp);
  constexpr float               [[redoc("`:lib>")]]ldexpf(float x, int exp);
  constexpr long double         [[redoc("`:lib>")]]ldexpl(long double x, int exp);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]log([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]logf(float x);
  constexpr long double         [[redoc("`:lib>")]]logl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]log10([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]log10f(float x);
  constexpr long double         [[redoc("`:lib>")]]log10l(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]log1p([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]log1pf(float x);
  constexpr long double         [[redoc("`:lib>")]]log1pl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]log2([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]log2f(float x);
  constexpr long double         [[redoc("`:lib>")]]log2l(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]logb([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]logbf(float x);
  constexpr long double         [[redoc("`:lib>")]]logbl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]modf([[redoc("^>")]]floating-point-type value, [[redoc("^>")]]floating-point-type* iptr);
  constexpr float               [[redoc("`:lib>")]]modff(float value, float* iptr);
  constexpr long double         [[redoc("`:lib>")]]modfl(long double value, long double* iptr);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]scalbn([[redoc("^>")]]floating-point-type x, int n);
  constexpr float               [[redoc("`:lib>")]]scalbnf(float x, int n);
  constexpr long double         [[redoc("`:lib>")]]scalbnl(long double x, int n);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]scalbln([[redoc("^>")]]floating-point-type x, long int n);
  constexpr float               [[redoc("`:lib>")]]scalblnf(float x, long int n);
  constexpr long double         [[redoc("`:lib>")]]scalblnl(long double x, long int n);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]cbrt([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]cbrtf(float x);
  constexpr long double         [[redoc("`:lib>")]]cbrtl(long double x);

  // [:en] [#c.math.abs], absolute values \
  [:zh_CN] [#c.math.abs]，绝对值
  constexpr int                 abs(int j);                             // [=freestanding]
  constexpr long int            abs(long int j);                        // [=freestanding]
  constexpr long long int       abs(long long int j);                   // [=freestanding]
  constexpr [[redoc("^>")]]floating-point-type abs([[redoc("^>")]]floating-point-type j);            // [=freestanding-deleted]

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]fabs([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]fabsf(float x);
  constexpr long double         [[redoc("`:lib>")]]fabsl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]hypot([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr float               [[redoc("`:lib>")]]hypotf(float x, float y);
  constexpr long double         [[redoc("`:lib>")]]hypotl(long double x, long double y);

  // [:en] [#c.math.hypot3], three-dimensional hypotenuse \
  [:zh_CN] [#c.math.hypot3]，三维斜边长
  constexpr [[redoc("^>")]]floating-point-type hypot([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y,
                                      [[redoc("^>")]]floating-point-type z);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]pow([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr float               [[redoc("`:lib>")]]powf(float x, float y);
  constexpr long double         [[redoc("`:lib>")]]powl(long double x, long double y);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]sqrt([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]sqrtf(float x);
  constexpr long double         [[redoc("`:lib>")]]sqrtl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]erf([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]erff(float x);
  constexpr long double         [[redoc("`:lib>")]]erfl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]erfc([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]erfcf(float x);
  constexpr long double         [[redoc("`:lib>")]]erfcl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]lgamma([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]lgammaf(float x);
  constexpr long double         [[redoc("`:lib>")]]lgammal(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]tgamma([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]tgammaf(float x);
  constexpr long double         [[redoc("`:lib>")]]tgammal(long double x);

  constexpr double[[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]ceil([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]ceilf(float x);
  constexpr long double         [[redoc("`:lib>")]]ceill(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]floor([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]floorf(float x);
  constexpr long double         [[redoc("`:lib>")]]floorl(long double x);

  [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]nearbyint([[redoc("^>")]]floating-point-type x);
  float               [[redoc("`:lib>")]]nearbyintf(float x);
  long double         [[redoc("`:lib>")]]nearbyintl(long double x);

  [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]rint([[redoc("^>")]]floating-point-type x);
  float               [[redoc("`:lib>")]]rintf(float x);
  long double         [[redoc("`:lib>")]]rintl(long double x);

  long int [[redoc("`:lib>")]]lrint([[redoc("^>")]]floating-point-type x);
  long int [[redoc("`:lib>")]]lrintf(float x);
  long int [[redoc("`:lib>")]]lrintl(long double x);

  long long int [[redoc("`:lib>")]]llrint([[redoc("^>")]]floating-point-type x);
  long long int [[redoc("`:lib>")]]llrintf(float x);
  long long int [[redoc("`:lib>")]]llrintl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]round([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]roundf(float x);
  constexpr long double         [[redoc("`:lib>")]]roundl(long double x);

  constexpr long int [[redoc("`:lib>")]]lround([[redoc("^>")]]floating-point-type x);
  constexpr long int [[redoc("`:lib>")]]lroundf(float x);
  constexpr long int [[redoc("`:lib>")]]lroundl(long double x);

  constexpr long long int [[redoc("`:lib>")]]llround([[redoc("^>")]]floating-point-type x);
  constexpr long long int [[redoc("`:lib>")]]llroundf(float x);
  constexpr long long int [[redoc("`:lib>")]]llroundl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]trunc([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]truncf(float x);
  constexpr long double         [[redoc("`:lib>")]]truncl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]fmod([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr float               [[redoc("`:lib>")]]fmodf(float x, float y);
  constexpr long double         [[redoc("`:lib>")]]fmodl(long double x, long double y);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]remainder([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr float               [[redoc("`:lib>")]]remainderf(float x, float y);
  constexpr long double         [[redoc("`:lib>")]]remainderl(long double x, long double y);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]remquo([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y, int* quo);
  constexpr float               [[redoc("`:lib>")]]remquof(float x, float y, int* quo);
  constexpr long double         [[redoc("`:lib>")]]remquol(long double x, long double y, int* quo);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]copysign([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr float               [[redoc("`:lib>")]]copysignf(float x, float y);
  constexpr long double         [[redoc("`:lib>")]]copysignl(long double x, long double y);

  double      [[redoc("`:lib>")]]nan(const char* tagp);
  float       [[redoc("`:lib>")]]nanf(const char* tagp);
  long double [[redoc("`:lib>")]]nanl(const char* tagp);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]nextafter([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr float               [[redoc("`:lib>")]]nextafterf(float x, float y);
  constexpr long double         [[redoc("`:lib>")]]nextafterl(long double x, long double y);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]nexttoward([[redoc("^>")]]floating-point-type x, long double y);
  constexpr float               [[redoc("`:lib>")]]nexttowardf(float x, long double y);
  constexpr long double         [[redoc("`:lib>")]]nexttowardl(long double x, long double y);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]nextup([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]nextupf(float x);
  constexpr long double         [[redoc("`:lib>")]]nextupl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]nextdown([[redoc("^>")]]floating-point-type x);
  constexpr float               [[redoc("`:lib>")]]nextdownf(float x);
  constexpr long double         [[redoc("`:lib>")]]nextdownl(long double x);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]fdim([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr float               [[redoc("`:lib>")]]fdimf(float x, float y);
  constexpr long double         [[redoc("`:lib>")]]fdiml(long double x, long double y);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]fmax([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr float               [[redoc("`:lib>")]]fmaxf(float x, float y);
  constexpr long double         [[redoc("`:lib>")]]fmaxl(long double x, long double y);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]fmin([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr float               [[redoc("`:lib>")]]fminf(float x, float y);
  constexpr long double         [[redoc("`:lib>")]]fminl(long double x, long double y);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]fmaximum([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]fmaximum_num([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]fminimum([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]fminimum_num([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);

  constexpr [[redoc("^>")]]floating-point-type [[redoc("`:lib>")]]fma([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y,
                                    [[redoc("^>")]]floating-point-type z);
  constexpr float               [[redoc("`:lib>")]]fmaf(float x, float y, float z);
  constexpr long double         [[redoc("`:lib>")]]fmal(long double x, long double y, long double z);

  // [:en] [#c.math.lerp], linear interpolation \
  [:zh_CN] [#c.math.lerp]，线性插值
  constexpr [[redoc("^>")]]floating-point-type lerp([[redoc("^>")]]floating-point-type a, [[redoc("^>")]]floating-point-type b,
                                     [[redoc("^>")]]floating-point-type t) noexcept;

  // [:en] [#c.math.fpclass], classification / comparison functions \
  [:zh_CN] [#c.math.fpclass]，分类/比较函数
  constexpr int  [[redoc("`:lib>")]]fpclassify([[redoc("^>")]]floating-point-type x);
  constexpr bool [[redoc("`:lib>")]]isfinite([[redoc("^>")]]floating-point-type x);
  constexpr bool [[redoc("`:lib>")]]isinf([[redoc("^>")]]floating-point-type x);
  constexpr bool [[redoc("`:lib>")]]isnan([[redoc("^>")]]floating-point-type x);
  constexpr bool [[redoc("`:lib>")]]isnormal([[redoc("^>")]]floating-point-type x);
  constexpr bool [[redoc("`:lib>")]]signbit([[redoc("^>")]]floating-point-type x);
  constexpr bool [[redoc("`:lib>")]]isgreater([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr bool [[redoc("`:lib>")]]isgreaterequal([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr bool [[redoc("`:lib>")]]isless([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr bool [[redoc("`:lib>")]]islessequal([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr bool [[redoc("`:lib>")]]islessgreater([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  constexpr bool [[redoc("`:lib>")]]isunordered([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);

  // [:en] [#sf.cmath], mathematical special functions \
  [:zh_CN] [#sf.cmath]，数学特殊函数

  // [:en] [#sf.cmath.assoc.laguerre], associated Laguerre polynomials \
  [:zh_CN] [#sf.cmath.assoc.laguerre]，连带拉盖尔多项式
  [[redoc("^>")]]floating-point-type assoc_laguerre(unsigned n, unsigned m, [[redoc("^>")]]floating-point-type x);
  float               assoc_laguerref(unsigned n, unsigned m, float x);
  long double         assoc_laguerrel(unsigned n, unsigned m, long double x);

  // [:en] [#sf.cmath.assoc.legendre], associated Legendre functions \
  [:zh_CN] [#sf.cmath.assoc.legendre]，连带勒让德函数
  [[redoc("^>")]]floating-point-type assoc_legendre(unsigned l, unsigned m, [[redoc("^>")]]floating-point-type x);
  float               assoc_legendref(unsigned l, unsigned m, float x);
  long double         assoc_legendrel(unsigned l, unsigned m, long double x);

  // [:en] [#sf.cmath.beta], beta function \
  [:zh_CN] [#sf.cmath.beta]，贝塔函数
  [[redoc("^>")]]floating-point-type beta([[redoc("^>")]]floating-point-type x, [[redoc("^>")]]floating-point-type y);
  float               betaf(float x, float y);
  long double         betal(long double x, long double y);

  // [:en] [#sf.cmath.comp.ellint.1], complete elliptic integral of the first kind \
  [:zh_CN] [#sf.cmath.comp.ellint.1]，第一类完全椭圆积分
  [[redoc("^>")]]floating-point-type comp_ellint_1([[redoc("^>")]]floating-point-type k);
  float               comp_ellint_1f(float k);
  long double         comp_ellint_1l(long double k);

  // [:en] [#sf.cmath.comp.ellint.2], complete elliptic integral of the second kind \
  [:zh_CN] [#sf.cmath.comp.ellint.2]，第二类完全椭圆积分
  [[redoc("^>")]]floating-point-type comp_ellint_2([[redoc("^>")]]floating-point-type k);
  float               comp_ellint_2f(float k);
  long double         comp_ellint_2l(long double k);

  // [:en] [#sf.cmath.comp.ellint.3], complete elliptic integral of the third kind \
  [:zh_CN] [#sf.cmath.comp.ellint.3]，第三类完全椭圆积分
  [[redoc("^>")]]floating-point-type comp_ellint_3([[redoc("^>")]]floating-point-type k, [[redoc("^>")]]floating-point-type nu);
  float               comp_ellint_3f(float k, float nu);
  long double         comp_ellint_3l(long double k, long double nu);

  // [:en] [#sf.cmath.cyl.bessel.i], regular modified cylindrical Bessel functions \
  [:zh_CN] [#sf.cmath.cyl.bessel.i]，规则变形圆柱贝塞尔函数
  [[redoc("^>")]]floating-point-type cyl_bessel_i([[redoc("^>")]]floating-point-type nu, [[redoc("^>")]]floating-point-type x);
  float               cyl_bessel_if(float nu, float x);
  long double         cyl_bessel_il(long double nu, long double x);

  // [:en] [#sf.cmath.cyl.bessel.j], cylindrical Bessel functions of the first kind \
  [:zh_CN] [#sf.cmath.cyl.bessel.j]，第一类圆柱贝塞尔函数
  [[redoc("^>")]]floating-point-type cyl_bessel_j([[redoc("^>")]]floating-point-type nu, [[redoc("^>")]]floating-point-type x);
  float               cyl_bessel_jf(float nu, float x);
  long double         cyl_bessel_jl(long double nu, long double x);

  // [:en] [#sf.cmath.cyl.bessel.k], irregular modified cylindrical Bessel functions \
  [:zh_CN] [#sf.cmath.cyl.bessel.k]，不规则变形圆柱贝塞尔函数
  [[redoc("^>")]]floating-point-type cyl_bessel_k([[redoc("^>")]]floating-point-type nu, [[redoc("^>")]]floating-point-type x);
  float               cyl_bessel_kf(float nu, float x);
  long double         cyl_bessel_kl(long double nu, long double x);

  // [:en] [#sf.cmath.cyl.neumann], cylindrical Neumann functions; \
  [:zh_CN] [#sf.cmath.cyl.neumann]，圆柱诺依曼函数；
  // [:en@~] cylindrical Bessel functions of the second kind \
  [:zh_CN@~] 第二类圆柱贝塞尔函数
  [[redoc("^>")]]floating-point-type cyl_neumann([[redoc("^>")]]floating-point-type nu, [[redoc("^>")]]floating-point-type x);
  float               cyl_neumannf(float nu, float x);
  long double         cyl_neumannl(long double nu, long double x);

  // [:en] [#sf.cmath.ellint.1], incomplete elliptic integral of the first kind \
  [:zh_CN] [#sf.cmath.ellint.1]，第一类不完全椭圆积分
  [[redoc("^>")]]floating-point-type ellint_1([[redoc("^>")]]floating-point-type k, [[redoc("^>")]]floating-point-type phi);
  float               ellint_1f(float k, float phi);
  long double         ellint_1l(long double k, long double phi);

  // [:en] [#sf.cmath.ellint.2], incomplete elliptic integral of the second kind \
  [:zh_CN] [#sf.cmath.ellint.2]，第二类不完全椭圆积分
  [[redoc("^>")]]floating-point-type ellint_2([[redoc("^>")]]floating-point-type k, [[redoc("^>")]]floating-point-type phi);
  float               ellint_2f(float k, float phi);
  long double         ellint_2l(long double k, long double phi);

  // [:en] [#sf.cmath.ellint.3], incomplete elliptic integral of the third kind \
  [:zh_CN] [#sf.cmath.ellint.3]，第三类不完全椭圆积分
  [[redoc("^>")]]floating-point-type ellint_3([[redoc("^>")]]floating-point-type k, [[redoc("^>")]]floating-point-type nu,
                               [[redoc("^>")]]floating-point-type phi);
  float               ellint_3f(float k, float nu, float phi);
  long double         ellint_3l(long double k, long double nu, long double phi);

  // [:en] [#sf.cmath.expint], exponential integral \
  [:zh_CN] [#sf.cmath.expint]，指数积分
  [[redoc("^>")]]floating-point-type expint([[redoc("^>")]]floating-point-type x);
  float               expintf(float x);
  long double         expintl(long double x);

  // [:en] [#sf.cmath.hermite], Hermite polynomials \
  [:zh_CN] [#sf.cmath.hermite]，埃尔米特多项式
  [[redoc("^>")]]floating-point-type hermite(unsigned n, [[redoc("^>")]]floating-point-type x);
  float               hermitef(unsigned n, float x);
  long double         hermitel(unsigned n, long double x);

  // [:en] [#sf.cmath.laguerre], Laguerre polynomials \
  [:zh_CN] [#sf.cmath.laguerre]，拉盖尔多项式
  [[redoc("^>")]]floating-point-type laguerre(unsigned n, [[redoc("^>")]]floating-point-type x);
  float               laguerref(unsigned n, float x);
  long double         laguerrel(unsigned n, long double x);

  // [:en] [#sf.cmath.legendre], Legendre polynomials \
  [:zh_CN] [#sf.cmath.legendre]，勒让德多项式
  [[redoc("^>")]]floating-point-type legendre(unsigned l, [[redoc("^>")]]floating-point-type x);
  float               legendref(unsigned l, float x);
  long double         legendrel(unsigned l, long double x);

  // [:en] [#sf.cmath.riemann.zeta], Riemann zeta function \
  [:zh_CN] [#sf.cmath.riemann.zeta]，黎曼 zeta 函数
  [[redoc("^>")]]floating-point-type riemann_zeta([[redoc("^>")]]floating-point-type x);
  float               riemann_zetaf(float x);
  long double         riemann_zetal(long double x);

  // [:en] [#sf.cmath.sph.bessel], spherical Bessel functions of the first kind \
  [:zh_CN] [#sf.cmath.sph.bessel]，第一类球面贝塞尔函数
  [[redoc("^>")]]floating-point-type sph_bessel(unsigned n, [[redoc("^>")]]floating-point-type x);
  float               sph_besself(unsigned n, float x);
  long double         sph_bessell(unsigned n, long double x);

  // [:en] [#sf.cmath.sph.legendre], spherical associated Legendre functions \
  [:zh_CN] [#sf.cmath.sph.legendre]，球面连带勒让德函数
  [[redoc("^>")]]floating-point-type sph_legendre(unsigned l, unsigned m, [[redoc("^>")]]floating-point-type theta);
  float               sph_legendref(unsigned l, unsigned m, float theta);
  long double         sph_legendrel(unsigned l, unsigned m, long double theta);

  // [:en] [#sf.cmath.sph.neumann], spherical Neumann functions; \
  [:zh_CN] [#sf.cmath.sph.neumann]，球面诺依曼函数；
  // [:en@~] spherical Bessel functions of the second kind \
  [:zh_CN@~] 第二类球面贝塞尔函数
  [[redoc("^>")]]floating-point-type sph_neumann(unsigned n, [[redoc("^>")]]floating-point-type x);
  float               sph_neumannf(unsigned n, float x);
  long double         sph_neumannl(unsigned n, long double x);
}
[codeblock:end]

[para]
[:en] The contents and meaning of the header [?libheader cmath] are a subset of
the C standard library header [?libheader math.h] and only the declarations shown
in the synopsis above are present, with the addition of a three-dimensional
hypotenuse function ([#c.math.hypot3]), a linear interpolation function ([#c.math.lerp]),
and the mathematical special functions described in [#sf.cmath].
[:zh_CN] 头文件 [?libheader cmath] 的内容和含义是 C 标准程序库头文件 [?libheader math.h]
的子集，仅提供以上纲要中展示的声明式，但添加了三维斜边函数（[#c.math.hypot3]），
线性插值函数（[#c.math.lerp]），和 [#sf.cmath] 中所述的各数学特殊函数。

[begin:note]
[:en] Several functions have additional overloads in this document, but they
have the same behavior as in the C standard library ([#library.c]).
[:zh_CN] 一些函数在本文档中有额外的重载，但它们的行为与 C 标准程序库中的相同（[#library.c]）。
[end:note]

[para]
[:en] For each function with at least one parameter of type [`[^floating-point-type]],
the implementation provides an overload for each cv-unqualified floating-point
type ([#basic.fundamental]) where all uses of [`[^floating-point-type]] in the
function signature are replaced with that floating-point type.
[:zh_CN] 对每个具有至少一个 [`[^floating-point-type]] 类型形参的函数，实现应当为
每个 cv 未限定的浮点类型（[#basic.fundamental]）提供一个重载，其函数签名中的每个
[`[^floating-point-type]] 均被替换为这个浮点类型。

[para]
[:en] For each function with at least one parameter of type [`[^floating-point-type]]
other than [`abs], the implementation also provides additional overloads
sufficient to ensure that, if every argument corresponding to a
[`[^floating-point-type]] parameter has arithmetic type, then every such argument
is effectively cast to the floating-point type with the greatest floating-point
conversion rank and greatest floating-point conversion subrank among the types
of all such arguments, where arguments of integer type are considered to have
the same floating-point conversion rank as [`double].
[:zh_CN] 除 [`abs] 之外，对每个具有至少一个 [`[^floating-point-type]] 类型形参的
函数，实现还提供额外的重载使得足以确保，如果对应于 [`[^floating-point-type]] 形参的
每个实参均为算术类型，则每个这样的实参均实际被转换为所有这些实参的类型中具有最大
浮点转换等级和最大浮点转换子等级的浮点类型，其中整数类型的实参被当做具有与
[`double] 相同的浮点转换等级。

[:en] If no such floating-point type with the greatest rank and subrank exists,
then overload resolution does not result in a usable candidate
([#over.match.general]) from the overloads provided by the implementation.
[:zh_CN] 如果不存在具有最大等级和子等级的浮点类型，则重载决议从实现提供的重载中
无法找到可用的候选（[#over.match.general]）。

[para]
[:en] An invocation of [`nexttoward] is ill-formed if the argument corresponding
to the [`[^floating-point-type]] parameter has extended floating-point type.
[:zh_CN] 如果对应于 [`[^floating-point-type]] 形参的实参具有扩充浮点类型，则对
[`nexttoward] 的调用非良构。
